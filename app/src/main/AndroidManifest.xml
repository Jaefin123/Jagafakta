<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jagafakta.jagafakta"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="SelectedPhotoAccess" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jagafakta"
        tools:targetApi="31">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


        <activity
            android:name=".ui.apk.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main/Home -->
        <activity
            android:name=".ui.apk.home.MainActivity"
            android:exported="false" />



        <!-- Register -->
        <activity
            android:name=".ui.apk.register.RegisterActivity"
            android:exported="false" />

        <!-- Scan -->
<!--        <activity-->
<!--            android:name=".ui.apk.scan.ScanActivity"-->
<!--            android:exported="false" />-->

        <!-- Result -->
        <activity
            android:name=".ui.apk.result.ResultActivity"
            android:exported="false" />

        <!-- History -->
        <activity
            android:name=".ui.apk.history.HistoryPredict"
            android:exported="false" />

        <!-- Profile -->
        <activity
            android:name=".ui.apk.profile.ProfileActivity"
            android:exported="false"
            tools:ignore="Instantiatable" />

    </application>
</manifest>
